name: mobile_app
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  # Maps & Location
  mapbox_maps_flutter: ^2.0.0
  geolocator: ^10.1.0
  permission_handler: ^11.3.0
  
  # UI Components
  google_fonts: ^6.1.0
  sliding_up_panel: ^2.0.0+1
  cupertino_icons: ^1.0.6

  # Keep these if you want to use them later, or remove if strictly reverting:
  # flutter_background_service: ^5.0.0 
  # tflite_flutter: ^0.10.4
  # flutter_sound: ^9.2.13
  # audio_session: ^0.1.21

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # Asset block removed